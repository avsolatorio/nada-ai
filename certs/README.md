# Place corporate / org root CA exports here (local only — do not commit `.pem` files).
#
# Example: export from macOS Keychain Access → Save as wbg-root-ca.pem
# Then uncomment SSL_CERT_FILE and the volume mount in docker-compose.qdrant.yml
#
# See docs/qdrant-pipeline-guide.md#tls-and-corporate-proxies
