# Optional OpenSearch overrides (passed through to compose `env_file` for the OpenSearch container).
# Override here as needed.

# Discovery data cache root (recommended for standalone ai4data discovery).
AI4DATA_DISCOVERY_DATA_PATH=
